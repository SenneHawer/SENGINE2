{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/SENGINE2",  // Path to your executable
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "preLaunchTask": "CMake: build",  // Ensure your build task is set
            "environment": [],
            "externalConsole": false
        }
    ]
}